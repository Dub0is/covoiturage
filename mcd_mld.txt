<MCD>
    <entités>
        
        utilisateur
        - uti_id (pk)
        - uti_nom

        profil
        - prof_id (pk)
        - prof_type

        trajet
        - traj_id(pk)
        - traj_départ
        - traj_destination
        - traj_moyen de transport
        - traj_date_heure_départ
        - traj_date_heure_arrivée
        - traj_arrêt_intermédiaire
        - traj_date_création
    </entités>

    <associations>

        proposer :
        - utilisateur 1,1
        - trajet 1,1

        inscrire :
        - utilisateur 1,N
        - trajet 1,N

        posséder :
        - utilisateur 1,N
        - profil 1,1
    </associations>
</MCD>

<MLD>
    utilisateur
    - uti_id int not null auto_increment primary key,
    - uti_nom varchar(100) not null 

    profil
    - prof_id int not null auto_increment primary key,
    - prof_type varchar(100) not null 

    trajet
    - traj_id int not null auto_increment primary key,
    - traj_depart varchar(50) not null ,
    - traj_destination varchar(50) not null ,
    - traj_moyen de transport varchar(50) not null ,
    - traj_date_heure_départ date time not null ,
    - traj_date_heure_arrivée date time not null ,
    - traj_arrêt_intermédiaire varchar(50) not null ,
    - traj_date_creation date not null ,

    proposer
    - pro_id int not null auto_increment primary key,
    - pro_utilisateur not null (fk)
    - pro_trajet not null (fk)
     cs3 foreign key pro_utilisateur references utilisateur(uti_id),
     cs4 foreign key pro_trajet references trajet(traj_id),
    primary key (rec_lecon,rec_client) 

</MLD>
